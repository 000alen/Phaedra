import React from "react";
import { IOrientation } from "./types";
export declare function computePosition(orientation: IOrientation, position: number): React.CSSProperties;
export declare function computeSize(orientation: IOrientation, size: number): React.CSSProperties;
